import { TranslatedatePipe } from './translatedate.pipe';

describe('TranslatedatePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatedatePipe();
    expect(pipe).toBeTruthy();
  });
});
